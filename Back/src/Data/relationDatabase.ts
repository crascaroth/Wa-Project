import { inputRawRelation } from "../Entities/Relation";
import { BaseDatabase } from "./BaseDatabase";

export class RelationDatabase extends BaseDatabase {
    public async associate(input: inputRawRelation){
        
    }
}