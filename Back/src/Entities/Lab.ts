export type AuthenticationData = {
    
}

export type InputRaw = {
    nome: string,
    endereco: string
} 

export type InputComplete = {
    id: string,
    nome: string,
    endereco: string
    
}